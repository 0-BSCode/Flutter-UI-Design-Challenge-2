import 'package:flutter/material.dart';

class FoodCategory extends StatelessWidget {
  final dynamic itemData;
  const FoodCategory({Key? key, required this.itemData}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
